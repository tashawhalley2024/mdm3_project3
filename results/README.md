# results/

Regression outputs and robustness tables from the secularism-women panel analysis.

---

## Key Finding

**Focal predictor:** `gri_religious_courts_norm` — Pew GRI separate religious courts score (normalised 0–1).

| Model | Coef | SE | p | N |
|---|---|---|---|---|
| T2 panel FE, no GDP | −0.011 | 0.004 | 0.002 | ~1,200 |
| T2 panel FE, with GDP | −0.008 | 0.003 | 0.003 | ~1,200 |
| Wild cluster bootstrap | −0.010 | — | 0.012 | — |
| Driscoll-Kraay HAC | −0.011 | 0.003 | <0.001 | — |
| WEF GGG external validation | −0.020 | — | 0.036 | 1,341 |

LOO jackknife: 170 country-drop runs, range [−0.01199, −0.00935], **0 sign flips, 0 non-significant**.

---

## Files

| File | Contents | Rows | Key columns |
|---|---|---|---|
| `results.csv` | All model tiers (T1–T2, P3–P10) — **WBL index** | ~325 | `tier`, `predictor`, `coef`, `se`, `pval`, `n` |
| `results_composite.csv` | All model tiers — **composite index** | ~550 | same |
| `results_wbl.csv` | WBL index results (backup copy) | ~347 | same |
| `index_comparison.csv` | Side-by-side coefficients, both indices | ~10 | `tier`, `coef_composite`, `coef_wbl`, `pval_composite`, `pval_wbl` |
| `index_comparison.md` | Plain-English explanation of index differences | — | — |
| `loo_jackknife.csv` | LOO jackknife — one row per dropped country | ~170 | `iso3`, `coef`, `se`, `pval` |
| `placebo.csv` | Placebo tests — courts on male/non-gendered outcomes | ~5 | `predictor`, `outcome`, `coef`, `pval` |
| `event_study.csv` | Event-study dummy coefficients | ~12 | `event_time`, `coef`, `se`, `pval` |
| `oster_sensitivity.csv` | Oster delta across Rmax values | ~7 | `rmax_multiplier`, `delta` |
| `spec_ladder.csv` | Specification ladder coefficients | ~7 | `spec`, `coef`, `se`, `pval` |

---

## Index Comparison Summary

The composite index (2007–2022, 13 components) shows a significant negative effect of religious courts (p = 0.002). The WBL index (2013–2022, legal rights + health) shows the same direction (β ≈ −0.005) but not significant (p ≈ 0.59). See `index_comparison.md` for the full explanation — the difference is primarily due to the shorter time window and lower within-country variation in the WBL index.

---

## Regenerating

Results are regenerated by running (from repo root):
```bash
python analysis/run_analysis.py
python analysis/compare_indices.py
```
